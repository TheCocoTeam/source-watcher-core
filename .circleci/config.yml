# This config was automatically generated from your source code
# Stacks detected: deps:php:.
version: 2.1
orbs:
  php: circleci/php@1
jobs:
  test-php:
    # PHP 8.5 line — keep in sync with source-watcher-dev-env/docker-compose.dev.yml (build.args.PHP_IMAGE).
    docker:
      - image: cimg/php:8.5-node
    steps:
      - checkout
      - php/install-packages
      - run:
          name: run tests
          command: composer run test:ci
workflows:
  build-and-test:
    jobs:
      - test-php
